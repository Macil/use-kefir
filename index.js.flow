/* @flow */

import type { Observable } from 'kefir';

declare export function useKefir<T>(
  stream: Observable<T, any>,
  initialValue: (() => T) | T,
  inputs: $ReadOnlyArray<mixed>
): T;

declare export function useSyncKefir<T>(
  stream: Observable<T, any>,
  initialValue: (() => T) | T,
  inputs: $ReadOnlyArray<mixed>
): T;
